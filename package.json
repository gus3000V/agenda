{
  "name": "express-vue-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix agend_fronted",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:agend_fronted": "npm run dev --prefix agend_fronted",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:agend_fronted\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
